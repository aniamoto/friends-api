en:
  errors:
    not_found: Object not found.
    friendships:
      blocked_user: Cannot create friendship with blocked user.
      unique_emails: Two unique emails are required.
  neo4j:
    errors:
      models:
        user:
          attributes:
            email:
              taken: Email has already been taken.
